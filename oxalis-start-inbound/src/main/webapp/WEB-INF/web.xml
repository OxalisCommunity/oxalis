<?xml version="1.0" encoding="UTF-8"?>
<web-app version="2.5"
         xmlns="http://java.sun.com/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_2_5.xsd">

    <description>SendRegning PEPPOL Reference implementation for an inbound access point</description>
    <display-name>oxalis</display-name>

    <!-- See http://logback.qos.ch/manual/loggingSeparation.html for further information on the configuration -->
    <!-- logback -->

    <!-- Improves the performance of Logback by avoiding JNDI lookups for each http request. -->
    <!---->
    <!--<filter>-->
        <!--<filter-name>LoggerContextFilter</filter-name>-->
        <!--<filter-class>ch.qos.logback.classic.selector.servlet.LoggerContextFilter</filter-class>-->
    <!--</filter>-->
    <!--<filter-mapping>-->
        <!--<filter-name>LoggerContextFilter</filter-name>-->
        <!--<url-pattern>/*</url-pattern>-->
    <!--</filter-mapping>-->


    <!-- Separates all logging requests from this application to the logging context configured in logback-oxalis.xml -->
    <!--<env-entry>-->
        <!--<env-entry-name>logback/context-name</env-entry-name>-->
        <!--<env-entry-type>java.lang.String</env-entry-type>-->
        <!--<env-entry-value>oxalis</env-entry-value>-->
    <!--</env-entry>-->

    <!-- Performs logging context destruction when this application is recycled or shutdown. This listener MUST -->
    <!--  be declared first so that its contextDestroyed() method is invoked last during application shutdown. -->
    <!--<listener>-->
        <!--<listener-class>ch.qos.logback.classic.selector.servlet.ContextDetachingSCL</listener-class>-->
    <!--</listener>-->

    <!-- Web Services stuff -->
    <listener>
        <listener-class>com.sun.xml.ws.transport.http.servlet.WSServletContextListener</listener-class>
    </listener>

    <listener>
        <listener-class>
            eu.peppol.inbound.server.ContextListener
        </listener-class>
    </listener>

    <servlet>
        <servlet-name>accessPointService</servlet-name>
        <servlet-class>com.sun.xml.ws.transport.http.servlet.WSServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>accessPointService</servlet-name>
        <url-pattern>/accessPointService</url-pattern>
    </servlet-mapping>

    <!--<servlet>-->
        <!--<servlet-name>ViewStatusMessages</servlet-name>-->
        <!--<servlet-class>ch.qos.logback.classic.ViewStatusMessagesServlet</servlet-class>-->
    <!--</servlet>-->

    <!--<servlet-mapping>-->
        <!--<servlet-name>ViewStatusMessages</servlet-name>-->
        <!--<url-pattern>/lbClassicStatus</url-pattern>-->
    <!--</servlet-mapping>-->


    <resource-ref>
        <description>JNDI DataSource for the DBMS persister</description>
        <res-ref-name>jdbc/oxalis</res-ref-name>
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
    </resource-ref>

    <session-config>
        <session-timeout>
            30
        </session-timeout>
    </session-config>
    <security-constraint>
        <display-name>SSL transport for accessPointService</display-name>
        <web-resource-collection>
            <web-resource-name>Secure area</web-resource-name>
            <description>Secure area</description>
            <url-pattern>/accessPointService</url-pattern>
            <http-method>GET</http-method>
            <http-method>POST</http-method>
        </web-resource-collection>
        <user-data-constraint>
            <description/>
            <transport-guarantee>CONFIDENTIAL</transport-guarantee>
        </user-data-constraint>
    </security-constraint>
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>
</web-app>
